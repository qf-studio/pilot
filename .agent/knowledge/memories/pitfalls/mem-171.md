# Pitfall: Box Claude OAuth token expiry silently wedges the headless daemon. Signature: da

## Summary
Box Claude OAuth token expiry silently wedges the headless daemon. Signature: daemon log shows only 'claude command failed: signal: killed' on classifiers + 'Backend execution failed error=unknown: exit status 1' with a CLEAN init in stdout_tail (session_id + tool list present) then death ~5min in; the real 401 authentication_error lives ONLY in stream recordings (~/.pilot/recordings/*/stream.jsonl), never daemon.log. All queued tasks burn generations to the repick cap and flip stalled. Two-part re-arm (restart-proof): (1) founder 'claude setup-token' (1yr) -> CLAUDE_CODE_OAUTH_TOKEN env in start-pilot.sh (env token beats dead 8h .credentials.json; verify sed didn't fuse export into exec line via cat -A); (2) remove pilot-blocked labels AND flip cap-stalled rows->failed by exact execution ID AND zero repick_backoff. Fixed 2026-08-23.

## Context
Discovered during development.

## Details
Box Claude OAuth token expiry silently wedges the headless daemon. Signature: daemon log shows only 'claude command failed: signal: killed' on classifiers + 'Backend execution failed error=unknown: exit status 1' with a CLEAN init in stdout_tail (session_id + tool list present) then death ~5min in; the real 401 authentication_error lives ONLY in stream recordings (~/.pilot/recordings/*/stream.jsonl), never daemon.log. All queued tasks burn generations to the repick cap and flip stalled. Two-part re-arm (restart-proof): (1) founder 'claude setup-token' (1yr) -> CLAUDE_CODE_OAUTH_TOKEN env in start-pilot.sh (env token beats dead 8h .credentials.json; verify sed didn't fuse export into exec line via cat -A); (2) remove pilot-blocked labels AND flip cap-stalled rows->failed by exact execution ID AND zero repick_backoff. Fixed 2026-08-23.

## Recommended Approach
Apply this knowledge when working on related topics.

## Related
- None documented

---
**Captured**: 2026-08-23
**Confidence**: 90%
**Concepts**: dispatcher, executor, authentication, oauth, stalled
