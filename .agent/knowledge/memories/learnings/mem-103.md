# Learning: Merge-gate map: handleCIPassed is the chokepoint, gates are escalate-only, require_ci is a RELEASE gate not a merge gate

## Summary
The pre-merge decision lives entirely in handleCIPassed (internal/autopilot/controller.go:1212-1277): SizeFloorReason (>500 net code lines, .agent/** excluded) and ScopeDriftReason (conventional-commit type/scope mismatch PR vs issue) in scope_guard.go, then env RequireApproval. Both gates are escalate-only by contract (scope_guard.go:15) — they can route to StageAwaitApproval, never silently bypass. require_ci (ReleaseConfig.RequireCI, GH-3994/PR #4096) is commonly conflated with this but only gates the post-merge release-train tagging path (controller.go:2061/3998/4369) — it has no bearing on whether the PR merges.

## Context
TASK-393 research (2026-07-09). Matters for Phase 5 (risk-score trust tiers): the score must extend the same escalate-only branch in scope_guard.go, adding escalations only, never suppressing SizeFloor/ScopeDrift/RequireApproval.

## Details
The pre-merge decision lives entirely in handleCIPassed (internal/autopilot/controller.go:1212-1277): SizeFloorReason (>500 net code lines, .agent/** excluded) and ScopeDriftReason (conventional-commit type/scope mismatch PR vs issue) in scope_guard.go, then env RequireApproval. Both gates are escalate-only by contract (scope_guard.go:15) — they can route to StageAwaitApproval, never silently bypass. require_ci (ReleaseConfig.RequireCI, GH-3994/PR #4096) is commonly conflated with this but only gates the post-merge release-train tagging path (controller.go:2061/3998/4369) — it has no bearing on whether the PR merges.

## Recommended Approach
New merge-risk signals go in scope_guard.go as XxxReason(files,...) functions consumed by handleCIPassed. Never wire merge logic to ReleaseConfig.RequireCI. See mem-093 for the adjacent StageReleasing drain-wedge incident.

## Related
- TASK-393
- `internal/autopilot/controller.go`
- `internal/autopilot/scope_guard.go`

---
**Captured**: 2026-07-09
**Confidence**: 85%
**Concepts**: autopilot, auto-merge, scope-guard, require-ci, risk-score, release
