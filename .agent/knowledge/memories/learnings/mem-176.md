# Learning: Failed-execution wedge is the identical-failure streak, not terminal dedupe

## Summary
#5008 narrative 'failed counts as terminal for dedupe' is wrong: HasTerminalCompletion (store.go:1290) never counts bare failed rows — they retry via nextRetryGeneration (#4372). The real forever-block: byte-identical env failures trip consecutiveIdenticalFailureThreshold=2 (dispatcher.go:1427) -> escalateStalledTask marks row stalled + applies pilot-blocked label, which the poller excludes silently with zero log lines. Diagnose wedged tasks at the streak/label layer first, not the terminal predicate.

## Context
TASK-485 research 2026-08-25, live repro pilot-ship-test-js#6 (missing ANTHROPIC_API_KEY, 4s, 0 tokens). GH-5139 rearm covers canceled rows only (LatestCanceledExecution hard status filter); no stalled/failed equivalent existed.

## Details
#5008 narrative 'failed counts as terminal for dedupe' is wrong: HasTerminalCompletion (store.go:1290) never counts bare failed rows — they retry via nextRetryGeneration (#4372). The real forever-block: byte-identical env failures trip consecutiveIdenticalFailureThreshold=2 (dispatcher.go:1427) -> escalateStalledTask marks row stalled + applies pilot-blocked label, which the poller excludes silently with zero log lines. Diagnose wedged tasks at the streak/label layer first, not the terminal predicate.

## Recommended Approach
When a task refuses re-dispatch: check the issue for pilot-blocked label and the ledger for status=stalled BEFORE suspecting HasTerminalCompletion/HasCompletedExecution. Env-class failures (0 tokens, no deliverable, instant exit) repeat identically and stall in exactly 2 attempts.

## Related
- TASK-485
- `internal/executor/dispatcher.go`
- `internal/memory/store.go`
- `cmd/pilot/rearm_canceled.go`

---
**Captured**: 2026-08-25
**Confidence**: 90%
**Concepts**: dispatch, retry, dedupe, stalled, pilot-blocked, env-failure
