# Learning: Route SaaS-build work through Navigator skills; v7-spike hook was silently breaking Read

## Summary
Founder wants nav-task/nav-pilot/nav-graph used, not manual gh/bash. A broken nav-spike PreToolUse probe (ephemeral /tmp install) hard-blocked the Read tool, which is part of why nav tooling looked 'avoided'.

## Context
2026-07-15 SaaS build: user flagged 'use nav tools please, you always avoid it'. Read failures traced to nav-spike@nav-spike-marketplace (path /tmp/nav-v7-spike/marketplace); s5_pretooluse.py wiped by /tmp clean -> python3 <missing> nonzero exit -> PreToolUse block on every Read + subagents.

## Details
spike_gate.py containment is fail-open but never runs because the file is missing at the OS exec layer. Durable fix filed as pilot issue alekspetrov/navigator#27 (guard command with [ -f ] || exit 0). Session unblocked by restoring hooks/*.py from ~/.claude/plugins/cache/nav-spike-marketplace/nav-spike/0.1.0/hooks/.

## Recommended Approach
Dispatch S-milestones via nav-task -> nav-pilot; record findings via nav-graph. When a nav skill fails, suspect a broken PreToolUse hook before falling back to raw bash. Any user-scope test hook must be fail-open.

## Related
- TASK-405
- `.agent/system/saas-roadmap.md`

---
**Captured**: 2026-07-15
**Confidence**: 90%
**Concepts**: navigator-tools, pretooluse-hook, nav-spike, read-tool, saas-build, fail-open
