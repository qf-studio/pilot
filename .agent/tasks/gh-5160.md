# GH-5160

**Created:** 2026-08-24

## Problem

On refusal, call h.client.AnswerCallback(ctx, callbackID, "⛔ Not an approver"), emit an h.log.Warn with request_id/user attrs (per telegram.go:496-500 pattern), skip RecordDecision, leave the pending map untouched, and return true (callback consumed).

## Acceptance Criteria

