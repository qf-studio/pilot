# GH-5361

**Created:** 2026-09-07

## Problem

GitHub Issue GH-5361: revise(autopilot): PR #5356 — remove unused test helper (lint), persist the own-close marker immediately after markSelfClosed, archive gh-5351 task doc

## Context

Revision of PR #5356 (issue #5351) on its existing branch `pilot/GH-5351`. Pre-merge review verdict: APPROVE-w-notes. CI `test` is green; `lint` fails on one finding and the autopilot size guard refused to spawn a fix issue, so this revision is filed by hand. Do not create a new branch and do not rebuild from main: check out `pilot/GH-5351` at its current head and commit on top.

## Changes

1. `internal/autopilot/gh5348_scope_mismatch_test.go` (~117): delete the unused method `hasRemoveLabel` on `scopeMismatchGHServer` (golangci-lint `unused`). It has no call sites since the test moved to the supersede-as-gate model.
2. `internal/autopilot/controller.go` (~3841): immediately after `markSelfClosed(...)` in `handleCIFailed`, persist the PR state (`c.persistPRState(prState)` or the equivalent already used at the `ProcessPR` tail ~2913) so a daemon death between the own close (~3846) and the tail persist cannot lose the marker and let the fallback apply `pilot-failed` + branch delete. Add a test that kills the flow after the close and before the tail persist and asserts the marker is on disk.
3. Move the task doc that PR #5356 added under the .agent tasks folder (file name gh-5351, markdown) into the .agent tasks archive folder.

## Acceptance

- [ ] `make lint` green (no `unused` finding), `make test` green.
- [ ] Marker persisted before the PR close returns; new test passes.
- [ ] Push to `pilot/GH-5351` so PR #5356 updates; no new PR.

---
*Manually filed revision issue mirroring the autopilot review-feedback format.*

<!-- autopilot-meta branch:pilot/GH-5351 pr:5356 iteration:1 -->

## Acceptance Criteria

