# GH-4490-1

**Created:** 2026-07-21

## Problem

## Subtask 1 of 4

**Parent Task:** GH-4490 - dashboard: queue cards stuck at running/100% after no-commit failure and external PR close — reconcile against executions table

## Objective

Dashboard card state reconciles periodically against the `executions` table (source of truth), not only via event transitions — a card whose execution row is terminal can never display `running`.

## Context

This is part of a larger task that has been decomposed for better execution.
Focus on this specific objective. Other subtasks will handle the remaining work.



## Acceptance Criteria

