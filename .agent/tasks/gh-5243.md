# GH-5243

**Created:** 2026-08-27

## Problem

In `docs/content/features/autopilot.mdx`: add a `ci_monitor.max_ci_fix_iterations` entry (default `3`, per `internal/autopilot/types.go:144-147`); for BOTH `max_ci_fix_iterations` and `review_feedback.max_iterations` state the closure semantics per mode explicitly — sequential → PR closed to unblock the queue; any other mode → PR held open with `pilot-needs-human` label, branch intact; correct the iteration-marker callout from `Autopilot-Iteration: N` to the actual `<!-- autopilot-meta branch:... pr:... iteration:N -->` format; add a note that `max_iterations: N` produces exactly N continuation issues (the Nth revision PR gets no further revision cycle). In `configs/pilot.example.yaml`: add both keys with their defaults and one-line closure-semantics comments. Verify by rendering the docs locally (or at minimum linting the yaml/mdx) and confirming the marker string matches what the code actually emits.

## Acceptance Criteria

