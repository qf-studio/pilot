# GH-3673

**Created:** 2026-06-26

## Problem

GitHub Issue #3673: feat(comms): bot module P5 — persona, voice scaffold, docs

## Context

Phase 5 (final) of the "Pilot Conversational Bot Module" plan. Full spec in the
repo: `.agent/tasks/TASK-377-bot-persona-docs.md`.

Depends on: #3671
Depends on: #3691

(Needs the Responder, retrieval, and issue-intake from the earlier phases — the
github poller skips this until #3671 and #3672 close.)

Finalize the bot's identity and document the new fast-vs-executor path. Voice
(Telegram/Slack calls) is deferred — this phase only ensures the seam exists.

## Implementation

- `bot.persona` honored across `Chat`/`Answer`/`DraftIssue` system prompts; sensible
  default if unset.
- `bot.voice.enabled` flag present (scaffold only). Confirm the comms chokepoint
  already consumes `IncomingMessage.VoiceText` so transcribed voice flows through the
  same intent→responder path; no call wiring in v1.
- `bot.rate_limit` reuses the existing `RateLimiter` to bound per-context LLM calls.
- Docs: `configs/pilot.example.yaml` `bot:` block fully commented; a
  `.agent/knowledge/memories/patterns/` entry "fast conversational path vs executor
  path" registered in `graph.json` (reference mem-036 test-at-the-seam lesson).
- `.agent/DEVELOPMENT-README.md` Current State refreshed with the shipped bot module.

## Acceptance

- [ ] Persona applied to all responder prompts; voice flag scaffolded; rate limit active.
- [ ] Knowledge-graph pattern entry + commented example config + Current State updated.
- [ ] Regression: `bot.enabled: false` ⇒ chat/question/intake revert to the executor path.
- [ ] `go build ./...`, `make lint`, `make test` clean.

## Refs

- Plan: `there-is-a-problem-inherited-fiddle.md`; spec: `.agent/tasks/TASK-377-*.md`
- mem-036: `.agent/knowledge/memories/pitfalls/bug_sdk_command_action_dropped.md`
- Depends on: #3671
- Depends on: #3691



## Acceptance Criteria

