# GH-4063

**Created:** 2026-07-08

## Problem

GitHub Issue GH-4063: refactor(cli): replace emoji with grot glyph vocabulary in CLI subcommand output

## Context

TASK-390 (PR #4061, released v2.234.0) moved the dashboard TUI + daemon console to the grot design system glyph vocabulary. CLI subcommand output still uses emoji — mechanical sweep, deliberately split out.

## Spec

The glyph table in the doc block of `internal/dashboard/grot_chrome.go` is the authoritative vocabulary:

- `●` active/online/running · `○` inactive/none · `◌` waiting/queued
- `▸` intake/selection · `✓` success · `✗` failure
- `!` warning · `↑` update available · `⟲` retry/restart · `·` separator · `→` transition

## Scope

- **In**: emoji in `cmd/pilot` subcommand output (`status`, `brief`, `replay`, `onboard`, `doctor`, `logs`, `metrics`, `budget`, etc.) and any `internal/` helpers that print to the CLI/terminal surface.
- **Out (do NOT touch)**: chat-adapter message content (Telegram/Slack/Discord user-facing messages keep emoji — different surface, per TASK-390 key decision), docs site, test fixture strings except where they assert the swept output.

## Acceptance

- [x] No emoji remain on the CLI/terminal surface (single-width semantic glyphs only)
- [x] Chat adapter message text unchanged
- [x] Tests updated where they assert swept output; full suite + lint green
- [x] Housekeeping: move `.agent/tasks/TASK-390-grot-dashboard-redesign.md` → `.agent/tasks/archive/`

## Acceptance Criteria

