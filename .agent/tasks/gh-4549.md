# GH-4549

**Created:** 2026-07-26

## Problem

Add a table-driven test in internal/autopilot/types_test.go (or the existing env-resolution test file) covering all five acceptance cases: flag-only, default-only, both (flag wins), neither (legacy fallback to stage), and unknown default name (error mentions available envs). Leave existing autopilot env tests untouched since they cover the absent default_environment path.

## Acceptance Criteria

