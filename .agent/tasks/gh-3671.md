# GH-3671

**Created:** 2026-06-26

## Problem

GitHub Issue #3671: feat(comms): bot module P3 — grounded Q&A retrieval

## Context

Phase 3 of the "Pilot Conversational Bot Module" plan. Full spec in the repo:
`.agent/tasks/TASK-375-bot-grounded-qa-retrieval.md`.

Blocked by: #3665

(Depends on the `Responder` + `internal/llm` from #3665 — do not start until that
PR merges. The github poller skips this until #3665 closes.)

`handleQuestion` (`internal/comms/handler.go:326`) uses the executor (90s) even for
shallow code questions. Add a bounded retrieval layer so code questions answer in
~2–4s, with executor fallback when the question is too broad. Ship behind
`bot.retrieval.enabled`.

## Implementation

- `internal/comms/retrieval.go`: bounded `filepath.WalkDir` of the active project,
  keyword/glob match, pick top `max_files`, read up to `max_bytes`, assemble a
  context block. Reuse an existing repo-search helper if present under
  `internal/executor`/`internal/memory`; else stdlib walk + `strings.Contains`.
  Returns `(contextBlock string, tooBroad bool)`.
- `Responder.Answer(ctx, history, question)`: retrieval → if `tooBroad`, signal
  fallback; else `llm.Answer` with `answerModel` (Sonnet) + the context block.
- `handleQuestion`: responder present → `Answer`; on `tooBroad`/error → existing
  executor path. Cite real file paths.
- Config `bot.retrieval`: `enabled`, `max_files` (default 8), `max_bytes` (24000).

## Acceptance

- [ ] Simple code questions answered via retrieval+LLM in ~2–4s, citing real files.
- [ ] Too-broad question (e.g. "explain the whole repo") falls back to the executor.
- [ ] Flag-gated by `bot.retrieval.enabled`; disabled ⇒ today's executor path.
- [ ] `go test ./internal/comms/...`, `go build ./...`, `make lint` clean.

## Refs

- Plan: `there-is-a-problem-inherited-fiddle.md`; spec: `.agent/tasks/TASK-375-*.md`
- Blocked by: #3665


## Acceptance Criteria

