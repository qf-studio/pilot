# GH-4459

**Created:** 2026-07-18

## Problem

Apply the subtask-1 helper at the three call sites in internal/autopilot/controller.go. Conflict rung (handleMergeConflict → closeAndReexecute, ~controller.go:3770): when API auto-rebase fails and conflicts are NOT go.mod/go.sum-only (existing attemptMechanicalConflictResolution already computes the file list), call escalateAndHold(pr, 'auto-rebase failed', ['needs-manual-rebase'], <conflicted-files-list>) instead of closeAndReexecute. Preserve the existing go.mod/go.sum auto-resolution path. CI-fail rung (handleCIFailed fix-issue path, ~controller.go:1820): reorder so the PR close only fires after the continuation fix issue clears preflight admission. If preflight declines or the create call errors, keep the PR open and call escalateAndHold(pr, 'CI-fix continuation declined at preflight', ['pilot-needs-human'], <failing-checks-summary>). A closed PR paired with a declined continuation is the exact dead-end that lost the GH-4415 fix twice. Size-guard rung (production_additions > floor path that produced the 16:25:41 WARN on PR #4419): replace the self-close with escalateAndHold(pr, 'CI-fix size guard fired', ['pilot-needs-human'], <failing-checks-summary>). Never close. Add regression tests per rung asserting: PR stays open, branch not deleted, escalateAndHold invoked with the right args, no re-execution issue created, self-close stamp NOT set. Depends on subtask 1 for the helper + attribution. Verifiable via existing autopilot controller test scaffolding.

## Acceptance Criteria

