# TASK-495: Pilot Fleet TO-BE alignment — Nelya's control-plane transfer (CloudFormation, ECS console, central auth)

**Status**: 🟢 **LOGIN GATE OPEN 2026-09-22 12:43Z — Nelya shipped all three legs in our order.** gRPC: internal NLB (quantflow VPC) TLS :4002 wildcard cert ALPN h2 → PrivateLink `vpce-svc-0b9e704000914a9d7` → console-api redeployed with the endpoint as template default; `GET /api/v1/me` via the edge = **401** (was 503 `bff disabled`). SES: `quantflow.studio` identity eu-central-1 VerifiedForSending + DKIM + MAIL FROM SUCCESS, task role may send only as `console@quantflow.studio`; **account sandboxed, production access requested** → flip `EmailEnabled` after. **UI LIVE 13:54Z at `console.pilotcloud.dev`**: workflow [ui#164](https://github.com/qf-studio/pilot-console-ui/issues/164)→PR#165 (APPROVE-w-defect) + stamp fix ui#167→PR#169 (APPROVE); deploy run 35736539861 green with all three edge checks. **~~BROWSER LOGIN PASSED 2026-09-22~~ — FALSE SUCCESS, found 2026-09-24: the deployed UI is the MOCK build (VITE_API_MODE unset → mock adapter, HTTP adapter tree-shaken out; live bundle has 0 `/api/v1/` refs). Register/login/Docs/Connections/Provision all ran against in-browser fixtures. Fix: [ui#170](https://github.com/qf-studio/pilot-console-ui/issues/170). The "unverified login allowed ⇒ email not a gate" conclusion is UNPROVEN until a real login is observed on the wire.** **09-24 real-console run:** registered → GitHub connected (after console-api SSM write grants 10:47Z) → Anthropic key → repo `pilot-ship-test-go` (CI added) → Provision 14:21Z → `provision_failed` on reconciler `iam:DetachRolePolicy` → Nelya granted 14:42Z → **NEXT: Deprovision → Provision → first tenant → Docs real tree** → price + Paddle L7; email track planned separately (gate claim withdrawn, see above). Code follow-ups: console#320 (skip detach on fresh role), console#319 (tick heartbeat/deadline, low prio). MFA enrol → browser login test → golden AMI + `tenant_factory=true` → Docs-page proof.
**Created**: 2026-09-06
**Assignee**: Pilot (code) · Nelya (stacks) · founder (decisions)

---

## Context

Nelya's artifact "Pilot Fleet TO-BE" (Slack `#infrastructure` msg `1788706191.406399`, 2026-09-06 16:49): the Pilot SaaS control plane moves into her infrastructure project as CloudFormation stacks (naming, tagging, KMS `alias/pilot-fleet`, least privilege, her deploy pipelines), the same way website/docs/auth went. Our CDK stacks (`pilot-cloud-infra`) stay until hers run, then retire.

**Founder decisions 2026-09-06:** (1) console runtime = **ECS** (API ×2 with `PILOT_CONSOLE_FLEET_RECONCILE=false` + reconciler ×1 via `consolectl run`); (2) **accept** her CloudFormation replacing CDK, with our review of templates; (3) **founder box stays** (runs the Pilot daemon; move = own project later); test tenant (S3-exit fixture, t3.large since 08-19) re-created in the new VPC; (4) **Q8**: her pipeline builds+deploys the console image from `prod-X.Y.Z` tags; tenant tarball + golden AMI stay ours.

**Her "Decided" items, accepted:** central auth `auth.quantflow.studio` (no second auth-service, no Redis, no JWT key on the console) · one SG per role, tenants trust the console SG only · new VPC `pilot-fleet` 10.50.0.0/16 (10.30/16 is pointer's).

## Observations verified (nav-research 2026-09-06, pilot-console origin/main)

| # | Claim | Verdict |
|---|---|---|
| 1 | Tenant roles read other tenants' `/tenants/*` params | **CONFIRMED** — `AmazonSSMManagedInstanceCore` (`internal/fleet/tenantres.go:30`) grants `ssm:GetParameter*` on `*`, additive over the org-scoped inline policy (373-381) |
| 2 | Volumes unencrypted | CONFIRMED — `internal/fleet/provisioner.go:454-465` no `Encrypted`/`KmsKeyId`; no root `BlockDeviceMappings` (504-524) |
| 3 | user-data runs `consolectl reconcile --loop` (nonexistent) | CONFIRMED — cloud-infra `userdata.sh.tmpl:119`; real entrypoint `cmd/consolectl/run.go` |
| 4 | Dockerfile ships only pilot-console | CONFIRMED |
| 5 | API+reconciler one process, no leader election | CONFIRMED; `PILOT_CONSOLE_FLEET_RECONCILE=false` disables cleanly → ECS split feasible today |
| 6 | ValidateToken per request, no cache, no custom CA | CONFIRMED — `internal/bff/middleware.go:70-99`; `internal/authclient/client.go:70-78`; only `_INSECURE` exists |
| 7 | Sessions plaintext, RLS dormant | CONFIRMED (documented; TASK-487) |
| 8 | SSM per secret per tenant per loop | PARTIAL — one `GetParametersByPath` per config push on drift (`configpush.go:358-393`), uncached across retries |
| 9 | SES ignores fleet region | CONFIRMED — `internal/email/ses.go:37-42` |
| 10 | No tags / release workflow | CONFIRMED |
| 11 | IdleWindow not env-wired | CONFIRMED — `reconciler.go:117-120` |

## Dispatched (all `pilot` + `no-decompose`, order-independent by design — see pitfall `pilot-decomposes-parent-issues-one-pr-instructions-not-honored`)

| Ask | Issue |
|---|---|
| Q2b encrypted volumes + root, `PILOT_CONSOLE_FLEET_VOLUME_KMS_KEY_ARN` | [pilot-console#259](https://github.com/qf-studio/pilot-console/issues/259) |
| Q2a inline SSM-agent statements + `PILOT_CONSOLE_FLEET_TENANT_ROLE_BOUNDARY_ARN` | [#260](https://github.com/qf-studio/pilot-console/issues/260) |
| Q2c tags from env (`Environment`, `Relation`, `Project`) | [#261](https://github.com/qf-studio/pilot-console/issues/261) |
| Q1 validation cache + gRPC CA/server-name | [#262](https://github.com/qf-studio/pilot-console/issues/262) |
| Q4/Q8 consolectl in image + `prod-X.Y.Z` release → GHCR | [#263](https://github.com/qf-studio/pilot-console/issues/263) → **PR#273 merged 09-06 23:20** (3rd run, after 2 false no-ops = pilot#5342); review APPROVE-w-notes → [#274](https://github.com/qf-studio/pilot-console/issues/274): **ECS reconciler task must use `entryPoint: ["/consolectl"], command: ["run"]`** (exec-form ENTRYPOINT; a command override silently starts a 3rd API server) · `latest`/dispatch guards · GHCR package is private on first push |
| Q6/Q7 + SES region (later, batched) | [#264](https://github.com/qf-studio/pilot-console/issues/264) |
| Q4 user-data `consolectl run` | [pilot-cloud-infra#49](https://github.com/qf-studio/pilot-cloud-infra/issues/49) |

## Post-merge reviews (2026-09-06, verdicts on the PRs)

| PR | Verdict | Follow-up |
|---|---|---|
| pilot-console #265 encryption | APPROVE-w-notes (root device name is a constant tied to the AL2023 golden AMI) | — |
| pilot-console #266 SSM policy | **REQUEST-CHANGES** — converge only runs on provision/replace; pre-#266 tenants keep `AmazonSSMManagedInstanceCore` until replaced; live SSM check not recorded | [#270](https://github.com/qf-studio/pilot-console/issues/270) (pilot) |
| pilot-console #267 tags | APPROVE (convergeTags has no diff guard; fine <100 tenants) | — |
| pilot-console #268 auth cache + TLS opts | APPROVE | — |
| pilot-console #269 idle/SES/preflight | APPROVE-w-notes (idle window inert: sleep adapters nil in both entrypoints) | — |
| cloud-infra #50 user-data | APPROVE | — |
| auth-service #509 per-app aud | APPROVE-w-notes — **refresh path drops per-client aud** (breaks on first refresh once enforcement is on) | [auth-service#512](https://github.com/qf-studio/auth-service/issues/512) (pilot, before any `JWT_AUDIENCE_ENFORCE=true`) |
| auth-service #510 TLS_ENABLED removal | APPROVE-w-notes | [auth-service#513](https://github.com/qf-studio/auth-service/issues/513) reminder (no pilot label) |
| pilot-console #271 role converge on tick (follow-up to #266) | APPROVE-w-notes — live evidence impossible until first AWS boot; no backoff for a persistently failing org | → first-deploy checklist below |
| auth-service #514 DPoP scheme, #515 refresh aud | reviewed by the auth-service agent (founder's call 09-06) | operator: set `TRUSTED_PROXY_CIDRS` SSM param (Nelya) |

## First-deploy validation checklist (add to the runbook 3-step validation)

- `aws iam list-attached-role-policies --role-name <tenant role>` on the re-created test tenant → no managed policy; SSM port-forward to the tenant still opens (proves #266/#271 narrowed policy).
- Tenant volume + root device show `Encrypted=true` with `alias/pilot-fleet` (#265).
- Tags `Environment=pilot-fleet`, `Relation`, `Project` on instance/volume/SG/role (#267).
- Console → central auth: a session validates once per TTL (cache hit metric), gRPC dial with `PILOT_CONSOLE_AUTH_GRPC_CA_FILE`/`_SERVER_NAME` (#268).
- ECS: reconciler task = `entryPoint: ["/consolectl"], command: ["run"]` (NOT a command override — exec-form ENTRYPOINT); API tasks `entryPoint: ["/pilot-console"]` with `PILOT_CONSOLE_FLEET_RECONCILE=false` (#263/#274).
- GHCR package `pilot-console` visibility/pull permission set once before the first ECS pull (created private).
- **Docs page renders a real `.agent` tree** for a fleet-provisioned tenant (login via gRPC :4002 → Tenants → Docs). This is the end-to-end proof TASK-466 never got; carried here from the superseded [infra#51](https://github.com/qf-studio/pilot-cloud-infra/issues/51) / TASK-498 (closed 2026-09-21 — task role `pilot-fleet-task-console-api` already has `ec2:DescribeInstances` + `/tenants/*` read + KMS decrypt).

## Late 09-06 / 09-07 additions

- **09-21 ~10:30Z — DOMAIN DECIDED: `pilotcloud.dev`** after search + US trademark clearance (memories `domain-pilotcloud-dev-keep-pilot-name`, `trademark-clearance-pilot-us-2026-09`); Nelya asked to register + start phase 2 (ALB/ACM/CloudFront), hosts `console.` + `api.`; nudged her on gRPC listener + SES first (`1789986971.468179`). Rename of the hosted product considered and rejected. **Open:** EU/UK trademark check (manual eSearch), MFA enrol, fresh AMI.
- **09-21 08:51Z — BOTH CONSOLE SERVICES LIVE ON pilot-fleet.** Nelya's real fix (`552c3c0`, 08:35Z): `AmiId` retyped to a pattern-checked `String` (the `AWS::EC2::Image::Id` type made CFN call `ec2:DescribeImages` as the deploying runner); the runner keeps no EC2 rights. Re-run [35579391317](https://github.com/qf-studio/pilot-console/actions/runs/35579391317) (`prod-0.1.0`, `tenant_factory=false`) → mirror/api/reconciler/verify all green; `console-api` UPDATE_COMPLETE 2/2, `console-reconciler` CREATE_COMPLETE 1/1 (`consolectl run: reconciler starting`), **10/10 CloudWatch alarms OK**. Pitfall worth a memory: a CFN parameter typed `AWS::EC2::Image::Id` (or any `AWS::*` typed param) is validated with the deployer's credentials — least-privilege runners fail at +2 s with a misleading AccessDenied. **Next:** (1) MFA enrol (founder) → port-forward `/health` + DB check (validation steps 1–2); (2) tell Nelya stable → she verifies alarms; (3) fresh golden AMI (2.259.3 is stale) → re-run with `tenant_factory=true` → step 3 (one tenant); (4) terminate old tenant + retire CDK stacks.
- **09-21 08:13Z — FIRST CONSOLE DEPLOY RUN** (workflow placed on pilot-console main `1f07b8c`; [run 35576741067](https://github.com/qf-studio/pilot-console/actions/runs/35576741067), `prod-0.1.0`, `tenant_factory=false`). **console-api LIVE**: `pilot-fleet-svc-console-api` CREATE_COMPLETE, 2/2 tasks, migrations applied at boot (silent on success), listening :8090; BFF 503 + email off as documented. **console-reconciler ROLLBACK_COMPLETE at +2 s**: `AccessDenied … ec2:DescribeImages` — CFN validates the `AWS::EC2::Image::Id` `AmiId` parameter as the deploying principal, and `pilot-fleet-runner` has no `ec2:*` (simulate: implicitDeny). Her fix: `ec2:DescribeImages` on `*` in `iam-runner-role.yml`. Posted to `#infrastructure` (`1789979094.724429`). Re-run the same workflow inputs once her role updates — the job deletes a ROLLBACK_COMPLETE stack itself. **MFA enrolment pending (founder).**
- **09-21 — NELYA DELIVERED (09-20 21:16–22:00Z): 14 `pilot-fleet-*` stacks green, ECS cluster `pilot-fleet` live (2 container instances + runner), ECR `pilot-fleet/pilot-console` (empty), console service templates written (`stacks/pilot-fleet/services/console-{api,reconciler}.yml`), MFA self-service on the operators group (`pilot-fleet-operators-own-mfa`, `65d150f`), `PILOT_CONSOLE_FLEET_ENV` derived from VpcName on her side.** Handover in `#pointer` (`1789942490.042549`) + zip `F0C30U7MRB5` (4 files: the pilot-console deploy workflow — NOT in her repo, only in the zip — the two service templates, the IAM boundaries). **Templates verified against all four 09-18 points: pass** (role names `pilot-fleet-task-console-{api,reconciler}` match the tenant key policy; API role has DescribeInstances + `/tenants/*` read + key Decrypt via SSM; DATABASE_URL is a composed SecureString `ValueFrom`; reconciler `entryPoint /consolectl` + `command run`; secrets driver defaults to `ssm` in code). **Deploying is OUR step**: copy `deploy-quantflow-aws.yml` into pilot-console, run with `image_tag prod-0.1.0`, `tenant_factory false`; after the 3-step validation re-run with `true`. **Caveats she flagged/we found:** (1) `AuthGrpcAddr` empty until her NLB TLS :4002 listener → BFF routes 503 → **browser login impossible; validation is API-token curl only** (`main.go:647`); (2) `GOLDEN_AMI_ID` = `ami-01ed3bb9600200ce4` = pilot **2.259.3** (08-16) — bake a fresh AMI before `tenant_factory true`; (3) her correction: `TRUSTED_PROXY_CIDRS` = ALB subnets **10.40.0.0/24, 10.40.1.0/24** since 09-08 (not 10.50/16 as we posted 09-07); (4) she asks us to run the **DPoP acceptance request** against auth.quantflow.studio (auth-service#508 AC); (5) orphan `mfa/iPh` not deleted. Still hers: SES identity `console@quantflow.studio`, NLB TLS listener; auth-service findings coming as text.
- **09-18 — TO-BE review against her DEPLOYED stacks + manifest correction sent** ([thread](https://quantflowstudio.slack.com/archives/C0BV37L87C1/p1789720014754879?thread_ts=1788706191.406399)). Her `pilot-fleet-*` stacks (8/12 live since 09-07) already cover every call the console proxy makes (`TenantSecrets`, `KmsEnvironmentKey`, `Ec2Describe`; tenant key policy names `pilot-fleet-task-console-*`); SG/NACL path console→tenant:9090 verified. **Four alignment points sent:** (1) `PILOT_CONSOLE_FLEET_ENV` must be `pilot-fleet` — our 09-07 manifest said `prod`, which her boundary denies at first `RunInstances` (pitfall `fleet-env-doubles-as-environment-tag-value`; decouple issue on pilot-console **unfiled**); (2) task-role prefix `pilot-fleet-task-console-` or the KMS key policy silently misses; (3) `DATABASE_URL` composed by an entrypoint wrapper must still `exec /consolectl run` for the reconciler; (4) boundary ≠ grant. **Ours:** MFA on `aleks` (none enrolled; her port-forward policy requires it) · fresh golden AMI (2.259.3) · port-forward SOP for the dynamic ECS host port · CDK retirement after her 3-step validation. Nelya silent since 09-07 in every channel. infra#51 (Opus-authored, targets the retiring CDK role) de-labelled + cancelled 09-15, close-as-superseded pending founder go.
- **pilot-console#45 ready-gate decoupling** (blocked since 07-24 on headers) re-specced 22:00 → PR#272 merged 23:13 → review APPROVE-w-notes (transient github:false flips the connection to `error`; consider a separate health field).
- **pilot-console#274** (pilot): README ECS contract wrong for the exec-form ENTRYPOINT + `latest`/dispatch guards. **#275** (pilot): `consolectl run` drops `TENANT_ROLE_BOUNDARY_ARN`, `IDLE_WINDOW`, `SECRETS_DRIVER` — boundary not applied on ECS until it merges.
- **ECS env manifest sent to Nelya** 09-07 00:27Z (TO-BE thread): task shapes, secrets split, AWS values, task-role IAM, the two caveats.
- **pilot#5342 executor false no-op**: PR#5345 pre-merge review **REQUEST-CHANGES** → drafted; revision [#5346](https://github.com/qf-studio/pilot/issues/5346) — first two runs classified *epic* (30 min) and timed out → 09-07 re-armed with `no-decompose` + step order in body. **pilot#5344** classifier OAuth 401 → PR#5347 merged 00:46.
- **09-07 11:29 — #5348 → PR#5349 merged** (SHA recorded+parsed, worktree from SHA/live branch, escalation when unfetchable = items 1–3 ✅); review REQUEST-CHANGES → [#5351](https://github.com/qf-studio/pilot/issues/5351) for the own-close marker + evidence-gated superseded. [#5350](https://github.com/qf-studio/pilot/issues/5350): decomposer ignores `no-decompose`, TUI paints subtasks red.
- **09-07 10:xx — FALSE SUCCESS caught:** pilot-console #275 (consolectl wiring: boundary/idle-window/secrets-driver) never landed — PR#276 failed on an unrelated flake, the CI-fix run rebuilt `pilot/GH-275` from main, PR#278 fixed only the flake, #277 done, #275 superseded. **Reopened #275 (pilot) with a cherry-pick note for the dangling commit 597710bc**; flow bug filed on pilot (see README). Until #275 lands the ECS reconciler applies NO permissions boundary. PR#279 (#274) APPROVE. **PR#5345 un-drafted per founder (merge partial fix; #5346 = follow-up on main).**
- **09-09 15:00Z — round closed, review coverage complete again.** PR#5401 (#5398) APPROVE-w-notes (GraphQL `lastEditedAt` works; `renamed` also re-arms cancelled/superseded — gate later) · PR#5402 (#5399) APPROVE-w-defects → [#5408](https://github.com/qf-studio/pilot/issues/5408) (`needs_human` missing from the memory store's terminal set — row overwritable; macOS-fragile 50 ms test; success-path hold precondition) · PR#5403 (#5400) APPROVE-w-defects → [#5409](https://github.com/qf-studio/pilot/issues/5409) (stale-label cleanup strips in-progress from live runs — the real trigger — unfixed; `OnPRCreated` trusts foreign issue numbers; late decline coexists with done; #5385 drops unexplained). Queue: #5407 #5408 #5409.
- **09-09 14:40Z — train v2.274.0 tagged 14:16Z, box on 2.274.0; sdk v0.38.2 14:01Z.** Queue drained (#5398 → PR#5401, #5399 → PR#5402, #5400 → PR#5403, #5404 → PR#5405, sdk#142 → PR#143). Reviewed: **PR#5405 APPROVE** (docs `s-maxage` live: `Hit from cloudfront`, `age: 62`; note SWR now applies to browsers too; markdown negotiation does not exist in `docs/` — if added, edge must key on `Accept`) · **sdk PR#143 APPROVE** → pilot pin bump + `ExecutionCheckerV2` impl [#5407](https://github.com/qf-studio/pilot/issues/5407). PR#5401/#5402/#5403 reviews running. Nelya: still no reply since 09-06.
- **09-09 10:10Z — FOUND Nelya's reply: it was in the DM thread** (`D0ADQMEBR6Y`, thread `1788728107.742929`, 09-06 23:49/23:50 CEST — never answered). **CloudFront is live since 09-06 23:30 CEST** in front of quantflow.studio + pilot.quantflow.studio (auth stays direct; direct ALB hits → 503; deploys invalidate `/*`; Brotli/HTTP3 at the edge). Her six items: B-07 docs `s-maxage` (NOT done — live header still `max-age=0`; source is `pilot/docs/next.config.mjs` → **pilot#5404** filed; pilot-docs#1 tracks) · B-06 `compress:false` (already set; keep) · C-07 images (minimumCacheTTL shipped 7.1.1; AVIF = qf-website#20 open) · GTM consent-gated (7.1.1; residual #32) · #18 shipped · **"the three additions never reached us" → sent verbatim now** (RSC cache key, compress:false at origin, HTML max-TTL cap 3600 s) + price class 100. Replied in the DM thread. Lesson: check DM threads, not only `#infrastructure`, when the founder says she replied.
- **09-09 09:50Z — Nelya pinged again** (TO-BE thread, msg `1788945801.223799`): 7-item list (GHCR pull method · deploy `prod-0.1.0` · SES identity · NLB ALPN · `TRUSTED_PROXY_CIDRS` confirm · CFN templates · compression/CSP + #447) + asked her to paste the auth-service findings attachment or file them as issues. No reply from her since 09-07.
- **09-09 09:35Z — all overnight PRs reviewed.** PR#5394 (#5386 flake) APPROVE (real de-flake: `receive.autogc false`; git ≥2.46 detached maintenance after push; SOP `bare-repo-test-tempdir-autogc-race`) · PR#5395 (#5391) APPROVE (bisect-verified) · PR#5396 (#5390) APPROVE (minor validator gaps). Pipeline residue → [#5400](https://github.com/qf-studio/pilot/issues/5400): #5386 ran straight through a preflight decline and closed done with `pilot-needs-clarification`; #5385 (no PR) auto-closed as delivered by its ORIGIN PR #5384. **Review coverage: every merged PR since 09-07 has a verdict.** Queue: #5398 running; #5399, #5400 queued; sdk#142 queued.
- **09-09 09:20Z — v2.273.2 PUBLISHED, box hot-upgraded 09:11:16Z (2.273.1 → 2.273.2)**; today's train slot already has a row ("recoverMissedTrainTick: skipping"). Reviews posted: **PR#5393 (#5381) APPROVE-w-defects** — loop fix sound, but Timeline `edited` events do not exist for body edits (feature dead code) + bot identity == operator on the founder PAT → [#5398](https://github.com/qf-studio/pilot/issues/5398) (GraphQL `lastEditedAt`), poller line → [studio-sdk#142](https://github.com/qf-studio/studio-sdk/issues/142). **PR#5392 (#5346) APPROVE-w-defects** — finalize ctx everywhere on the success path + push+hold on the error path, but the success-path gate-retry loop still re-invokes after a timeout, `needs_human` outcome unknown to the dispatcher, unpushed salvage branch deleted by cleanup → [#5399](https://github.com/qf-studio/pilot/issues/5399). #5398 already picked (09:13Z).
- **09-09 09:07Z — v2.273.2 tagged by hand on `718fd676`** (founder approved 09-08 15:05Z; carrier had marked the 09-08 train scope `released` before verification failed → `release_missing` alert, no re-tag). Release run 34332940716 → watch publish + box self-upgrade from 2.273.1. Overnight the queue drained: **#5392 (#5346 executor finalization ctx), #5393 (#5381 re-arm fix-forward, Timeline edited events), #5394 (#5386 CI flake), #5395 (#5391), #5396 (#5390)** all merged — reviews running. #5386 ended `pilot-needs-clarification` + `declined-preflight` before its PR (check). Queue empty at 09:07Z.
- **09-08 14:52Z — INCIDENT CLOSED: revert PR#5388 merged (`f8532806`, test 7m39s green).** Main clean of the loop; box never ran it. Worktree + branch removed. Fix-forward #5381 next in queue. Watch: carrier must re-tag from ≥ f8532806 (cancelled v2.273.2 tag deleted; `bug_goreleaser_cancellation` says later tags may stall — if no tag by the next tick, check `scope release carrier` lines). #5376 keeps `pilot-done` from PR#5380; #5381 is the real fix.
- **09-08 15:00Z — four more post-merge reviews posted**: PR#5382 (#5377) APPROVE · PR#5383 (#5375) APPROVE-w-notes (helper is `mutateIssueLabels`, not `guardPilotLabels` — that name is pilot-console's; no stranded issues to clean) · PR#5384 (#5379) APPROVE-w-defects → [#5390](https://github.com/qf-studio/pilot/issues/5390) (footer regex matches quoted/fenced text anywhere in the body — ungated since #5384) · PR#5387 (#5378) APPROVE-w-defects → [#5391](https://github.com/qf-studio/pilot/issues/5391) (`escalateAndHold` never clears `SizeGuardHoldActive` → a terminal-cap hold can be revived). Revert PR#5388 still in CI (watcher merges on green).
- **09-08 12:21–14:46Z — INCIDENT: self-re-arming loop merged, contained before release.** PR#5380 (#5376) pre-merge review found a BLOCKING defect (`updated_at` counts the bot's own stall comment/label as re-arm evidence → every stalled task retries forever, paid) — autopilot merged it 12:21:14Z, one minute before the draft attempt. Founder chose the hand revert: worktree `.claude/worktrees/revert-5380`, `git revert e25714eb` → **PR#5388** (build/vet/re-arm tests green; pushed `--no-verify`). Fix-forward rewritten as **#5381** (new branch from main; footer removed) — next in queue after #5346. Meanwhile the train: post-merge CI red at 215ee8c (flaky `TestRunner_DirtyWorktreeAfterNoCommitRetry_AutoPreserved`; autopilot filed #5385/#5386) held the carrier; main green at d6fe3422 14:29 → **carrier tagged v2.273.2 at 14:45:34Z with #5380 inside** → **release run 34240312995 cancelled + tag deleted 14:46:23Z, no Release object published, box still 2.273.1**. Residue: main has `docs: sync version to v2.273.2` (#5389) with no such release — cosmetic; pitfall `bug_goreleaser_cancellation` warns later tags may stall → watch the carrier after PR#5388 merges. Merged meanwhile, reviews running: #5382 (#5377), #5383 (#5375), #5384 (#5379), #5387 (#5378).
- **09-08 12:10Z — nav-research on the abandoned-PR class (#5378) → body rewritten with the real design** (third flag-gated redrive in `processAllPRs`, like `reAdoptHeldRebasePR`; new `SizeGuardHoldActive` on `PRState`; `OnPRCreated` stays registration-only; orphan reconciler cannot rescue tracked PRs; no `FailureReason` field). Queued row for #5378 cancelled on the box + `pilot` re-labelled so the run captures the new body (body is captured at dispatch — learning `hand-merge-leaves-pilot-needs-human-and-stale-body-capture-at-dispatch`). **Second defect found: the `autopilot-meta` footer is parsed only under the `autopilot-fix` label** (`handlers.go:814`) → hand-filed revisions #5361/#5374 defaulted to `pilot/GH-<n>` (stray branch `pilot/GH-5374` deleted) → [#5379](https://github.com/qf-studio/pilot/issues/5379); pitfall `autopilot-meta-footer-ignored-without-autopilot-fix-label`. Pattern memory `autopilot-failed-stage-revival-is-a-flag-gated-redrive-before-processpr`. #5351's leftover `pilot-needs-human` stripped.
- **09-08 11:50Z — queue check: idle 1h40m.** (1) **#5375 gate-held 20× on my own wrong path** `internal/autopilot/cleanup.go` (does not exist; the stale-label sweep lives in controller/epic_reconcile/scope_reconcile) → escalated `pilot-needs-human` at 11:49:46Z, 14 s before my body fix; label stripped 11:51Z, body rewritten in prose. **Second same-class self-inflicted hold in 24 h (#5353 yesterday) — run the backtick path check BEFORE `gh issue create`, not after.** (2) **Autopilot never re-evaluated PR#5356** after the size-guard failure (stage `failed`; zero autopilot lines for pr=5356 after 10:26Z even with CI green + un-drafted) → **squash-merged by hand 11:50:04Z (`7582722e`, clean message, reviewed twice)**; #5351 auto-closed; revision issues #5361/#5374 closed by hand (not auto-closed by the PR). Defect to file: a `failed`-stage PR that later goes green is never re-tracked.
- **09-08 10:35Z — review coverage audit: every PR merged since 09-07 has a verdict** (pilot 13 incl. #5357 version-sync skipped as docs-only; console 3; sdk 2). **PR#5356 rebase (#5374) went green → drafted to block auto-merge → rebase reviewed APPROVE-w-notes** (merge + 3-line adaptation; 104 autopilot tests pass in a throwaway worktree; no main reverts) → un-drafted; follow-up [#5377](https://github.com/qf-studio/pilot/issues/5377) (review path lacks the persist between `markSelfClosed` and the close). Rule reaffirmed: when a revision lands on a held PR, draft it until the rebase diff is reviewed.
- **09-08 10:00–10:10Z — overnight drain + first console release.** **`prod-0.1.0` CUT** (founder "go next"; tag on `542986d` via API; release run green 10:06Z; GHCR `prod-0.1.0`/`sha-542986d`/`latest`, package private; GitHub Release with tarball) → posted to Nelya with the PAT-vs-Actions-access choice restated (no reply from her since 09-07). Note: pilot-console has NO tag ruleset (README says org-admin restriction) — founder item. Overnight: 8 PRs merged, **all reviewed** — #5363 (prior), #5365 w-notes, #5366 w-notes, #5367 APPROVE, #5368 w-notes (comment+alert pre-existed since #4462; 09-07 alert loss likely nil `alertsEngine`), #5371 APPROVE, #5372 w-defects → [#5375](https://github.com/qf-studio/pilot/issues/5375) (gate never strips `pilot`/`pilot-in-progress`), **#5373 = #5359 DELIVERED** (CI-fix continuation carried the commits byte-identically — #5348 fix proven on the review path's twin). **PR#5356 CONFLICTING** (`owner_death.go` 7 hunks + `markSelfClosed` signature vs #5372) → revision [#5374](https://github.com/qf-studio/pilot/issues/5374) iteration 2. **#5346 never re-picked for 33 h**: body-edit re-arm is not evidence for `sweepStalledRearm`; `claim_lost_drops=76`, 16-min window re-armed forever → label cycle `retry-1→retry-ready` 10:07Z; defect [#5376](https://github.com/qf-studio/pilot/issues/5376); pitfall `stalled-rearm-sweep-ignores-body-edit-loops-forever`.
- **09-07 16:10Z — PR#5363 (#5353) pre-merge APPROVE-w-defects**: fix proven (early return on `alreadyStalled`, `dispatcher.go:2443`), but shipped tests pass with the fix reverted (same-second stamps; interleave step comments-only) → [#5364](https://github.com/qf-studio/pilot/issues/5364). Session close: pilot queue = #5354 #5355 #5358 #5359 #5360 #5361 #5362 #5364 + #5346 retry-1; PRs to review as they land.
- **09-07 16:00Z — receipts digest first scheduled delivery ✅** (`generating receipts digest` 16:00:00.000Z → `receipts digest delivered` 16:00:00.067Z, Telegram chat 283716179). #5353 → **PR#5363** open (pre-merge review running). #5361 body was blanked by a failed sed edit for ~2 min and restored with the branch-only path rewritten in prose (gate-safe).
- **09-07 15:45Z — PR#5356 pre-merge review APPROVE-w-notes** (all four #5351 items verified on the branch; crash window: marker persisted only at ProcessPR tail; review-revision path `handleReviewRequested` ~4325 still has the #275 shape). Revision issue **[#5361](https://github.com/qf-studio/pilot/issues/5361)** (lint helper delete + persist-after-markSelfClosed + archive task doc; `autopilot-meta branch:pilot/GH-5351 pr:5356 iteration:1`). Follow-up **[#5362](https://github.com/qf-studio/pilot/issues/5362)** (review path). Open pilot queue now: #5353 (running) · #5354 · #5355 · #5358 · #5359 · #5360 · #5361 · #5362 · #5346 (retry-1).
- **09-07 15:35–15:50Z — queue check ("autopilot got conflicts?" — no):** auth-service **v0.73.0 released 14:02Z, Nelya's deploy green 14:32Z, live healthy** → `TRUSTED_PROXY_CIDRS=10.50.0.0/16` posted to her. studio-sdk **v0.38.1 tagged 14:01Z** (sdk#140 → PR#141 APPROVE-w-notes; URL branch ignores owner/repo) → pin bump [#5358](https://github.com/qf-studio/pilot/issues/5358). pilot v2.273.1 tagged 14:15Z, box hot-upgraded 14:19Z. **#5353 was HELD by the base-presence gate 13:29→15:34Z (18 holds)** on my own backticked `internal/executor/store.go` (real path `internal/memory/store.go`) — body fixed, running 15:39Z; #5354/#5355 queued behind it. The 12:52Z "stalls" = boot reconciliation after the hot upgrade; board prints `created_at` (learning `board-stalled-timestamp-is-created-at-after-self-upgrade-reconciliation`). **#5351/PR#5356**: run 1 shipped the PR but was recorded `no_op` (merge-base unresolvable → [#5359](https://github.com/qf-studio/pilot/issues/5359)); one lint `unused` finding → **CI-fix size guard refused a fix issue → `pilot-needs-human`, no comment/alert** ([#5360](https://github.com/qf-studio/pilot/issues/5360); pitfall `ci-fix-size-guard-holds-own-pr-on-one-lint-annotation`). Recovery: pre-merge review of PR#5356 running → manual revision issue with `autopilot-meta` footer.
- **09-07 12:55Z — #280 + sdk#138 shipped and reviewed same hour.** pilot-console **PR#281 APPROVE-w-notes, delivery VERIFIED on origin/main** (`wiring.go:39` single helper; boundary/idle-window/secrets-driver reach `consolectl run`; cherry-pick of 597710bc byte-identical; 5 green check-runs) → **first `prod-*` tag can be cut (founder, org-admin tag rule)**. studio-sdk **PR#139 APPROVE-w-defects: does NOT prevent the incident** — bare `#n` regex accepts the CI-fix template's `Original Issue: #275` line → follow-up [studio-sdk#140](https://github.com/qf-studio/studio-sdk/issues/140) (closing-keyword-only + real-template fixture); **no sdk tag/pin bump until #140 lands**.
- **09-07 12:40Z — owed reviews posted:** PR#5343 (#5272 stalled re-arm) **APPROVE-w-defects** → follow-up [#5353](https://github.com/qf-studio/pilot/issues/5353) (`escalateStalledTask` re-stamps `completed_at` on the alreadyStalled path, so a poller pickup before the sweep hides the re-arm evidence forever; claim-lost log is Info not WARN; `gh-5272.md` to archive). PR#5352 (#5350) **APPROVE-w-defects** → follow-up [#5354](https://github.com/qf-studio/pilot/issues/5354) (running in-process subtasks still flipped to failed by the dead-owner sweep — no executions row, ID not in live set; widen acceptance-header pattern). **#5350's "ignores no-decompose" claim was wrong:** #5348 was picked with `[bug pilot]`; the label arrived 7 min later. Rule: label `no-decompose` before/with `pilot`, never after. Pitfall memory corrected.
- **09-07 12:50Z — #275 UNPICKABLE, refiled as [pilot-console#280](https://github.com/qf-studio/pilot-console/issues/280) (picked 12:53Z).** Box log 10:35:41Z: `github-sdk-poller` "Merged PR found via branch lookup" `pilot/GH-275` → "marking as done" — PR#278 (the #277 CI-fix) merged on that head branch, so every re-arm was reverted in one poll cycle. Upstream defect filed [studio-sdk#138](https://github.com/qf-studio/studio-sdk/issues/138) (pilot-labeled; pin bump on pilot after release). Pitfall memory `poller-branch-lookup-marks-issue-done-from-another-issues-pr`. "Needed from you" list posted to Nelya (PAT vs Actions access · CFN templates · SES identity · NLB ALPN · compression/CSP · #447).
- **09-07 12:37Z — Nelya's five questions answered in the TO-BE thread** (founder decisions: sender `console@quantflow.studio`; **tenant binary = golden AMI only, `PILOT_CONSOLE_FLEET_BINARY_S3_URI` unset, no tenant S3/KMS grant, her `pilot-fleet-s3-app-data` not used for tenants**). Facts posted: GHCR package not created until the first `prod-*` tag (none exist yet; PAT must be classic `read:packages`, or grant `pilot-fleet` repo Actions access); gRPC addr unset ⇒ API boots, BFF routes 503 (`main.go:705`); TLS via NLB TLS listener + `_SERVER_NAME` + ALPN `HTTP2Preferred`; #275 unmerged (daemon re-stamped `pilot-done` 10:35Z after strip at 10:31Z — #5351 class), first prod tag will carry it; tarball upload has never had a workflow (manual `aws s3 cp --sse aws:kms` from the founder box; key `releases/vX.Y.Z/pilot-linux-amd64.tar.gz`). Still owed: `TRUSTED_PROXY_CIDRS` key after the 16:00 tag; her answer PAT-vs-Actions-access. Her 09-06 18:43 CEST file `auth-service-findings-2026-09-06.md` NOT yet read (Slack attachment).
- **09-07 morning:** pilot-console #275 → PR#276 failed CI → fix #277 (old-binary body carried `Depends on: #275`; #275 closed to unblock). #274 parked on a backticked `/entrypoint.sh` → reworded, unparked. **Box rebuilt from main + restarted 09:18Z** (v2.273.0-23-ge054b01c: deadlock fix, re-adoption cutoff, classifier fix, docs sync all live); receipts digest scheduler active.

## 09-21 phase 2 log (domain → edge)

- **13:12 CEST** domain sent (`1789989147.511449`). **13:19** registered in Route 53 (auto-renew, expiry 2027-09-21). **11:42–11:50Z** `-domain` (ALB + wildcard ACM in both regions) · `-cdn` (CloudFront `E6U3NSSVQ0PTQ`, bucket `pilot-fleet-s3-console-ui`) · `-domain-config` (WAF + `console` record) CREATE_COMPLETE. Nelya posted nothing — found by probing AWS.
- **Shape**: she took the single-origin option. OIDC issuer = `https://console.pilotcloud.dev`; Paddle webhook host = `console.pilotcloud.dev` under `/api/` (both sent, recorded by her).
- **Gap 1 (ours to find, hers to fix)**: ALB :443 had only a fixed 503 — the console-api template's TargetGroup/ListenerRule/LB binding sit behind `HasDomain` (`DomainStack` param), and the workflow passes only `ImageTag`/`JobId`/`AlertsTopicArn`, so run 35605374961 (green) still left the stack in minimal mode. Fix: she flipped the template default to `DomainStack=pilot-fleet-domain` (S3 template 15:58Z) and applied the stack by hand with the secret (stack `UPDATE_COMPLETE` 14:11Z). **`OriginVerifySecret` cannot be a dynamic reference** (CFN rejects SecureString refs in listener-rule conditions and ssm resolvers in defaults) → workflow must read `/pilot-fleet/shared/CDN_ORIGIN_VERIFY` → [console#317](https://github.com/qf-studio/pilot-console/issues/317) (`pilot`). Ordinary re-runs are safe (`deploy` keeps previous param values); the recreate-after-rollback path is what #317 protects.
- **Gap 2 (my probe, her defect)**: S3 behaviours forwarded the viewer Host header → S3 NotFound for everything. Fixed + verified by her (test index.html on `/`, `/tenants/abc`, nested → 200). Deep links via a viewer-request function rewriting extension-less paths to `/index.html` (not a custom error response); `/assets/*` misses stay 403. Bucket empty again.
- **Health routes** (`/health`, `/ready`, `/live`) are root-mounted → unreachable via CloudFront (only `/api/*` goes to the ALB); ALB TG health check uses them directly (2/2 healthy). Edge proof = `GET /api/v1/me` → `{"error":"bff disabled"}` 503 from Go. `GET /api/v1/billing/config` → 404 through the edge (billing routes not mounted in this image/env — check before Paddle L7).
- **Pitfall** (to memory): CFN template conditions keyed on parameters the workflow never passes = silent minimal mode with a green run; check `describe-stacks` parameters, not job status. Template on S3 is KMS-locked to the runner (`kms:Decrypt` denied for `user/aleks`) — cannot verify her template edits from the laptop.

- **09-22 14:52 CEST — NELYA: ALL THREE LEGS LIVE** (after the purpose→exists→build→verify brief at 13:03 CEST, msg `1790075021.748009`; she had not understood the earlier one-liners). Verified from here: edge 401 · PrivateLink endpoint `available` (`vpce-0d5a1d5a612caae51`) · SES identity SUCCESS ×3, `ProductionAccessEnabled=false`. She flagged the origin-secret line as still pending — stale, PR#318 merged 09-21. Founder decision same day: **email stays on SES** (memory `console-email-transport-ses-not-email-service-repo`). UI workflow: ours, ui#164, to her spec (assets immutable, index no-cache, invalidate `E6U3NSSVQ0PTQ`, edge verify incl. SPA deep-link + BUILD_SHA).

- **09-22 13:28–13:54Z — CONSOLE UI DEPLOYED.** ui#164 → PR#165 merged 13:28Z (review APPROVE-w-defect). First deploy 35734393645: site live, `sha_ok=0` — **the CloudFront viewer function rewrites every extension-less path to index.html, so `/BUILD_SHA` returned the HTML** (my #166 blamed caching — wrong; Pilot's PR#168 closed unmerged, #166 closed superseded). Correct fix ui#167 → PR#169 merged 13:53Z (stamp `build-sha.txt`, no-cache, invalidated, `.js` probe). Second deploy 35736539861 **green**, `/build-sha.txt` = be61794 from the edge. Two base-presence self-holds today (#164 `SITE=https://…dev`, #167 `/build-sha.txt`) — memory mem-175 updated: hold self-releases after ~3 ticks on v2.276.1; un-backtick every non-repo dotted token before filing.

- **09-24 14:42Z — Nelya granted `iam:DetachRolePolicy` + `iam:ListAttachedRolePolicies` on `role/pilot-tenant-*`** to the reconciler role (template + live, role-only; task boundary widened the same way; `AttachRolePolicy` still denied by design; simulated on `pilot-tenant-425f9f99-…`: detach/list allowed, attach denied, other roles denied). Task def rev 6 unchanged. **Next: founder Deprovision (row is `provision_failed`, never retried) → wait terminated → Provision → watch ≥2 ticks → CloudTrail `RunInstances` + EC2 in `subnet-0529475cb9b9ec41c` from `ami-01ecd7cefa1e6f316` → post tenant id to Nelya (she verifies account-side) → Docs real tree.**
- **09-24 14:22Z — FIRST REAL PROVISION REACHED THE FACTORY; failed at tenant-role convergence.** Nelya added the console-api write grants at 10:47Z (silently, 7 min after the ask); GitHub token (13:41Z) + Anthropic key (13:44Z) written as SecureStrings under `/tenants/425f9f99-…/`; repo relinked to `qf-studio/pilot-ship-test-go` (not polled by the box; CI added by hand over SSH — `gh` OAuth token lacks the `workflow` scope; run 36009602317 green). `POST /api/v1/instances/provision` → 201 at 14:21:07Z; reconciler acted 14:22:11Z: `CreateRole pilot-tenant-425f9f99-…` OK (boundary + inline `tenant-secrets`/`tenant-ssm-agent`), then **`iam:DetachRolePolicy` AccessDenied** — `pilot-fleet-task-console-reconciler` has no such grant, and `convergeRolePolicy` detaches `AmazonSSMManagedInstanceCore` unconditionally even on a role it just created. Row → `provision_failed` (no reconciler retry; founder must Deprovision → Provision). Asked Nelya for `iam:DetachRolePolicy` + `iam:ListAttachedRolePolicies` on `role/pilot-tenant-*` (same thread); code fix filed [console#320](https://github.com/qf-studio/pilot-console/issues/320). **Operator error on my side:** log/CloudTrail checks ran seconds before the tick, I read the silence as a hung loop, forced a reconciler redeploy (harmless) and filed [console#319](https://github.com/qf-studio/pilot-console/issues/319) on a false premise — body corrected to a pure observability ask (heartbeat + per-tick deadline). Lesson: the reconciler acts within one 60 s tick and logs only on error; wait ≥2 ticks and read the instance's Activity events before diagnosing.
- **09-24 10:40Z — FIRST REAL CONSOLE RUN, BLOCKED AT CONNECT GITHUB: console-api task role cannot write tenant secrets.** Founder registered on the real build, GitHub token (laptop `gh` OAuth token, `repo` scope; existing SSM `/pilot/GITHUB_TOKEN`/`GITHUB_PAT` are nelyaparfenova-dev fine-grained PATs) validated live, then PUT failed: `ssm:PutParameter` AccessDenied on `parameter/tenants/425f9f99-…/GITHUB_TOKEN` for `pilot-fleet-task-console-api`. Role has only SSM `Get*` on `/tenants/*` + KMS `Decrypt/DescribeKey` on key `0578f83b…`; `internal/secrets/writer.go` needs `PutParameter`/`DeleteParameter`/`DeleteParameters` + `kms:Encrypt`. Asked Nelya in `#infrastructure` (template-first). Repo chosen by founder: `qf-studio/pilot` (advised a low-risk repo; founder's call). Add to the first-deploy checklist: **write-path grants on console-api (secrets writer) — read-only grants pass every smoke test until the first real connect.**
- **09-24 — FALSE SUCCESS UNCOVERED: console.pilotcloud.dev serves the MOCK UI.** Founder reported "instance is active" after connect GitHub → Anthropic key → Provision; screenshot showed `inst_1 · us-east-1 · Pilot 1.4.2 · events 100d ago` = `mockAdapter.ts` fixture. Verified: no tenant EC2, zero `RunInstances` from the reconciler in CloudTrail, reconciler alive on task def 6 with the factory on and no error logged; live bundle (build-sha be61794) has 0× `/api/v1/`, 3× `us-east-1`, 2× `inst_1`; `adapter.ts` defaults to mock unless `VITE_API_MODE=http`, and the deploy workflow never sets it. Edge → console-api is healthy (`GET /api/v1/me` = 401 JSON). Consequence: the 09-22 login/Docs entry below and today's provision were never real; mock has no persistence, so the pasted Anthropic key never left the tab. Fix filed [ui#170](https://github.com/qf-studio/pilot-console-ui/issues/170) (set the mode + bundle guard). Pitfall memory mem-195. **SHIPPED same morning: ui#170 → PR#171 (autopilot-merged 06:52Z, review APPROVE-w-notes) → deploy run 35966679422 green with the new guard; live bundle 961bd42 = HTTP build (46× `/api/v1/`, 0 fixture markers).** Real-console run now: founder re-registers (no account exists in the real DB) → login → connect GitHub → Anthropic key → Provision → first tenant from `ami-01ecd7cefa1e6f316` → Docs real tree.
- **09-22 ~15:00Z — FIRST BROWSER LOGIN THROUGH THE EDGE (founder-run).** `/register` → `/login` → dashboard → Docs page, all on `console.pilotcloud.dev`. auth-service has `EMAIL_ENABLED=false`, so the account is unverified and **login still succeeded → email verification is not enforced; the email track (relay route under `/api/v1/`, template token secret, auth-service env, UI reset/verify pages) is a normal-priority follow-up, not a gate.** Docs page rendered its UI; the *real `.agent` tree* proof still needs a fleet tenant (console DB empty) — stays on the checklist. SES prod access granted by Nelya (50k/day) — flag NOT flipped, see 14:52 entry.

- **09-23 — OLD CDK ESTATE TEARDOWN STARTED (founder call 09-22).** Nothing in the 10.30 `FleetVpcStack` family serves either Pilot (founder box = terminal Pilot in `pilot` VPC; console = `pilot-fleet`). Done from here: test tenant `i-0a3bf271d598196ca` terminated, its data volume `vol-0a9665cc633ae041f` deleted. **Blocked on rights** (`user/aleks`: no `cloudformation:DeleteStack`, no `sts:AssumeRole` on CDK roles, no `ec2:DeleteSnapshot`) → handed to Nelya in `#infrastructure` (`1790148632.984189`): delete `TenantBaseStack` → `ControlPlaneStack` (RDS `DeletionPolicy: Snapshot`) → `FleetVpcStack`; keep `CDKToolkit` until pilot-cloud-infra is archived; 16 orphan DLM snapshots listed for her. **Keep `DataLifecycleStack`**: its DLM policy (`pilot:data-volume=true`, 7-day) is the founder box's only daily data-volume backup (`vol-068f78767202b20aa`, 200 GB) — re-home before deleting. Saving ≈ $190/mo. Pitfall to remember: a "dead" CDK stack can still carry a live cross-estate control (tag-targeted DLM policy) — check `TargetTags` before deleting.

- **09-23 10:26 CEST — Nelya's drift check on the `pilot` family (the estate we keep).** Founder-box role `pilot-agent` carried a hand-added inline policy `pilot-agent-tenant-secrets` (`ssm:GetParameter*` on `/tenants/*`, added 07-23 for the S2 e2e when tenants used the shared instance profile; markers said "fold into TenantBaseStack later" — never done). CFN-managed role ⇒ next `pilot-agent-role` deploy would silently drop it. Verified the box never reads `/tenants/*` (config, units, full daemon log) → **told her to remove the drift, no template statement.** The dead tenant's 3 params (`/tenants/2a4bdc46-…/`): `GITHUB_TOKEN` (classic PAT) already *Bad credentials*, `ANTHROPIC_API_KEY` (`…QAAA`) already *invalid* and ≠ the box's `/pilot/ANTHROPIC_API_KEY` → nothing to revoke, delete with the estate. Her rule adopted: anything we need on her stacks goes into her templates via her pipeline first. Her backup stack is written; first snapshot of `vol-068f78767202b20aa` tonight + restore test before `DataLifecycleStack` deletion. Learning: hand-applied IAM on CFN-managed roles is a time bomb — record it as a template change or expect it to vanish on the next deploy.

- **09-23 ~12:00Z — founder-box data volume is UNENCRYPTED; Nelya built an encrypted copy chain** (snapshot → copy under `alias/pilot` KMS → `vol-0fd96ec9e734ee4cf`, 200 GB gp3, eu-central-1a, point-in-time ~11:00Z) for a **restore test** (attach as 2nd device, mount ro, verify) and proposed a **10–15 min swap window** (daemon stop → fresh snapshot → encrypted copy → attach at `/dev/sdf` in place → start). My attach attempt failed `CustomerKeyHasBeenRevoked` = **KMS grant, not the key**: `alias/pilot` policy allows only `user/nelya` + service principals; `user/aleks` has no `kms:CreateGrant/Decrypt` → EBS can't create the grant. Volume untouched, nothing mounted. Asked her to attach or grant (`kms:CreateGrant/Decrypt/DescribeKey` via `ec2.eu-central-1.amazonaws.com` + `ec2:Attach/DetachVolume`). **Gotcha for the swap:** the copy keeps the live XFS UUID `1a5c104a-…` → side-by-side needs `-o ro,nouuid`; in-place swap needs no fstab change (mounted by UUID). Daemon = tmux session `pilot` via `start-pilot.sh`; queue idle → window today. Verify plan: `PRAGMA integrity_check`, execution/autopilot counts vs live, recordings count; after swap: `mount -a`, start, board + last rows; confirm decrypt survives stop/start.

- **09-23 09:47–09:51Z — FOUNDER-BOX DATA VOLUME SWAPPED TO ENCRYPTED (≈4 min pause, founder-approved).** Restore test on Nelya's encrypted copy `vol-0fd96ec9e734ee4cf` passed first (integrity ok; executions 5024 = live, same latest row; recordings 20111 = live; repos 3 = 3; mounted `ro,nouuid`). Nelya then granted `user/aleks` group `pilot-founder-box-operators` (attach/detach on `pilot:role=founder-box` volumes + `alias/pilot` via EC2) → future restore tests are ours. Swap: TUI `C-c` → 0 procs in 5 s → a stray `go` process held `/var/lib/pilot` (`fuser -km`) → unmounted → "go" → she snapshotted post-stop (`snap-0b77e591bc4ce15d4` → encrypted `snap-0a2643dc9a19fde89`) → new `vol-02e1ae374973f0779` at `/dev/sdf` → `mount -a` (same XFS UUID, no fstab change) → daemon started, `2.276.1`, 1 proc, queue 0, autopilot scanning; integrity ok, 5024 rows. Old `vol-068f78767202b20aa` detached, kept as rollback for a few days. Nightly snapshots now encrypted at rest. SOP-worthy: stop → fuser/umount → detach-swap → mount -a → start → verify (integrity + counts + version + queue metric).

- **09-23 12:41 CEST — Nelya's teardown sweep DONE:** inline policy drift removed; the 3 dead-tenant params + 16 orphan snapshots deleted; `TenantBaseStack` gone; her duplicate fleet backup policy removed (single owner `pilot-data-lifecycle` + our `DataLifecycleStack` until tonight's snapshot confirms); `ControlPlaneStack`: RDS deleted with final snapshot `controlplanestack-snapshot-controlplanedatabase955544f1-kbcy1jcwmnal`, ALB gone; SG delete blocked by the dead tenant's factory-created SG (`9090` rule) + IAM role `pilot-tenant-2a4bdc46-…` (both queued), `FleetVpcStack` after. **Answered 13:20 CEST: old control-plane DB = July–Aug test data only → delete the snapshot + schedule KMS key `a0e6dc65`.** (Her question sat 40 min unanswered — the 3-min watch was on a different thread; lesson: watch the channel, not one thread.)

- **09-23 13:21 CEST — OLD ESTATE CLOSED.** Nelya's summary (`1790162354.977609`): TenantBase/ControlPlane/FleetVpc stacks + VPC 10.30 deleted, dead-tenant SG/role/params gone, orphan + RDS final snapshots deleted, key `a0e6dc65` scheduled 09-30; her duplicate backup policy removed (single owner `policy-01ba06ee281502274`, 01:30 UTC, keep 7); deletion queue tagged `pilot:delete-after`: 09-26 old rollback volume `vol-068f78767202b20aa` + its snapshots + test volume, 10-01 the encrypted migration snapshots. **pilot-cloud-infra ARCHIVED 13:21 CEST (founder call)** after closing infra#35/#36 (isolation harness, superseded); `CDKToolkit` released to her. Hygiene: the box config still lists `pilot-cloud-infra` as a Pilot project — drop it at the next config edit/restart (poller on an archived repo is harmless but noise).

## Operator / founder items

- ~~Domain pick~~ **DECIDED 2026-09-21: `pilotcloud.dev`** (memory `domain-pilotcloud-dev-keep-pilot-name`); sent to Nelya `1789989147.511449` → she registers + starts phase 2. Marketing/docs stay on pilot.quantflow.studio.
- After #259–#263 merge: post the ECS task-definition env manifest to Nelya (promised on Slack).
- Review Nelya's CloudFormation templates when she shares them (sequence step 2).
- Cut the first pilot-console `prod-*` tag once #280 (ex-#275) lands; post the tag to Nelya; then GHCR pull access (her choice: PAT or Actions access).
- Read Nelya's `auth-service-findings-2026-09-06.md` (Slack file, TO-BE channel 09-06 18:43 CEST) — not triaged.
- Founder box move: separate future task; NOT part of cut-over.

## Out of Scope

- Sessions plaintext / RLS activation (TASK-487 documented; security-review item).
- Local JWT validation instead of gRPC (cache is the agreed mitigation).
- Golden AMI / tenant tarball pipeline changes.

## Refs

- Slack thread: `#infrastructure` C0BV37L87C1 `1788706191.406399` (her post) · our reply 2026-09-06
- TASK-405 (SaaS program), TASK-490 (S4 minimal in-VPC console — superseded on the infra side by this transfer; code decisions stand)
- pilot-cloud-infra user-data fix: [infra#49](https://github.com/qf-studio/pilot-cloud-infra/issues/49)

---

**Last Updated**: 2026-09-21
