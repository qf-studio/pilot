# GH-5052

**Created:** 2026-08-20

## Problem

In internal/executor/, add base_presence.go starting from PR#5047's closed-branch content where it was correct (seam placement pre-ExecStatusRunning, StageBasePresenceHeld ledger event, log + progress emit, no-op fast path for ref-free bodies). Define the probe interface with FileExistsOnDefaultBranch and IssueOrPRState signatures that match internal/adapters/github/repo_state.go (including its merged-vs-closed distinction). Implement hold logic for BOTH ref shapes: (a) 'Depends on: #N' where #N is an open PR, and (b) #N is an issue whose attached PR is open-unmerged (canonical incident form — requires IssueOrPRState attached-PR lookup). Implement backtick-path hold via FileExistsOnDefaultBranch (404⇒absent, probe error ⇒ fail-open + log). In dispatcher.go, wire the checker BEFORE the ExecStatusRunning transition and before any backend call; run the GH-4656-class issue-closed/superseded revalidation BEFORE or INSIDE the hold path so closing the held issue always releases; after a configurable bounded cycle count, apply pilot-needs-human AND park the row (dequeue/hold-state) so the queue head advances and other project tasks proceed — do NOT reset-and-loop; escalated rows resume only via existing needs-human resolution. Reset the project|task hold counter to 0 on natural claim after a hold. Keep gh-CLI shellout as an explicit fallback only when no probe is registered. Add unit tests that drive basePresenceChecker.Check directly through a fake probe (open-PR ref ⇒ hold; issue-with-open-PR ⇒ hold; issue-with-merged-PR ⇒ no hold; closed-unmerged-PR ⇒ no hold; missing path ⇒ hold; probe error ⇒ fail-open logged) plus PR#5047's real incident-body extraction fixtures (GH-5021, ui#120/#124/#139 — reproduce verbatim). Add dispatcher-level fake-tracker multi-tick tests for: dual-shape hold-then-release when the underlying PR merges; persistent-hold escalation firing once then queue-head advance to a second queued task for the same project (assert escalation does not re-fire every N cycles); closing the held issue releases across ticks; natural-release counter reset (held N<bound cycles, claims, later retry generation starts at 0); zero probe calls asserted for ref-free bodies; ledger event + log + progress observability all asserted. This must be a single subtask since splitting within a Go package causes merge conflicts and the checker + dispatcher hold path are tightly coupled. Only touch internal/adapters/github/repo_state.go if signatures genuinely need adjustment — prefer adapting the executor interface to the merged helpers.

## Acceptance Criteria

