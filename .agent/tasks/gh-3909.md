# GH-3909

**Created:** 2026-07-06

## Problem

In internal/executor/prompt_builder.go, after the existing BuildPrompt assembly completes (past the Navigator /nav-loop prefix at :134-151), conditionally append a '## Known pitfalls from project memory' block listing recalled memories with type, summary, and confidence, ending with 'Heed these before implementing.' Gate on: config.Executor.MemoryInjection.Enabled && !task.LocalMode && hasNavigator && len(recalled) > 0. Cap at min(config.MaxMemories, 5) entries AND ~1500 chars total (truncate list, not mid-entry). Preserve byte-identical output when disabled / LocalMode / no graph / no matches — assert with a golden-style test. Table-driven tests: injection appears for a task mentioning 'epic decomposition' against a fixture graph, LocalMode skip, disabled-config skip, char cap enforcement, and Navigator prefix untouched (regression guard for mem-004). Depends on subtasks 1 and 2.

## Acceptance Criteria

